# Database Connection
# PostgreSQL database URL (required)
DATABASE_URL=postgresql://user:password@localhost:5432/pi_business_hub

# Server Configuration
PORT=5000
NODE_ENV=production

# Session Secret (generate a random string for production)
SESSION_SECRET=your-random-session-secret-here

# Optional: For Replit Auth integration (if deploying on Replit)
# REPL_ID=your-repl-id
# REPLIT_PROFILE_URL=your-replit-profile-url
