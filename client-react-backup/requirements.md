## Packages
recharts | Dashboard charts and analytics visualization
framer-motion | Smooth animations for page transitions and UI elements
date-fns | Date formatting for tables and tasks

## Notes
- Auth is handled via Replit Auth (useAuth hook)
- API endpoints follow the shared/routes.ts contract
- Images should use Unsplash URLs with descriptive comments
